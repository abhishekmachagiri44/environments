name:  NodeJS CI
on:
  # push:
  #   branches:
  #     - "*"
  workflow_call:
    inputs:
      target-env:
        required: true
        type: string
      
jobs:
  build:
    runs-on: ubuntu-latest
    environment: ${{ inputs.target-env }}
    outputs:
      matrix1: ${{ steps.demo.outputs.sample }}
      matrix2: ${{ steps.demo1.outputs.sample1 }}   
    steps:
    - name: Run tests
      run: echo "${{ vars.RUNNERS }}"
    - name: Run tests2
      run: echo "${{ vars.RUNNERS2 }}"
    - name: demo
      id: demo
      run: echo "sample=${{ vars.RUNNERS }}" >> $GITHUB_OUTPUT
    - name: demo1
      id: demo1
      run: echo "sample1=${{ vars.RUNNERS2 }}" >> $GITHUB_OUTPUT

  example_matrix:
    runs-on: ${{ matrix.version }}
    needs: build
    environment: ${{ inputs.target-env }}
    env:
      runner_tag: ${{ matrix.version }}
      runner_tag_RUN: ${{vars.RUN}}
    strategy:
      matrix:
        version: ${{fromJSON(needs.build.outputs.matrix1)}}
    steps:
    - name: devil
      run: echo "${{env.runner_tag_RUN}}"

  example_matrix1:
    runs-on: ${{ matrix.version }}
    needs: build
    environment: ${{ inputs.target-env }}
    strategy:
      matrix:
        version: ${{fromJSON(needs.build.outputs.matrix2)}}
    steps:
    - name: devil
      run: echo "sample2"
